{
  "name": "app-1-interview-prep",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/*.js",
    "lint:fix": "eslint src/*.js --fix",
    "format": "prettier --write src/*.js",
    "prepare": "husky",
    "build:prod": "vite build --outDir dist",
    "build:dev": "vite build --outDir dist-dev"
  },
  "devDependencies": {
    "eslint": "^9.35.0",
    "husky": "^9.1.7",
    "prettier": "^3.6.2",
    "sass": "^1.92.1",
    "vite": "^7.1.2"
  }
}
